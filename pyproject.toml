[project]
name = "denpa-bot.py"
version = "0.1.0"
description = "porting the denpa bot to python"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "discord-py>=2.6.4",
    "python-dotenv>=1.2.1",
]
